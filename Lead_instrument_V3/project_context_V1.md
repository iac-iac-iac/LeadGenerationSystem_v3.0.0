# Lead Generation System - MVP v1.0

**Дата создания:** 05.02.2026  
**Последнее обновление:** 05.02.2026 13:38  
**Статус:** MVP ЗАВЕРШЁН ✅

---

## 📋 Описание проекта

Автоматизированная система обработки лидов для интеграции с Битрикс24. Система выполняет очистку, валидацию, дедупликацию CSV файлов с контактами и подготовку данных для импорта в CRM.

### Ключевые функции

- ✅ Загрузка и объединение множественных CSV файлов
- ✅ Валидация российских номеров телефонов (мобильные и городские)
- ✅ Удаление дубликатов по телефонам
- ✅ Экспорт в формате Битрикс24 с распределением менеджеров
- ✅ Аналитика результатов лидогенерации
- ✅ Визуализация метрик (графики, отчёты Excel)
- ✅ GUI интерфейс с темами (светлая/тёмная)
- ✅ История обработок в SQLite
- ✅ Логирование всех операций
- ✅ Предпросмотр обработанных данных

---

## 🏗️ Архитектура проекта

```
lead-generation-system/
│
├── main.py                          # Точка входа
│
├── config/
│   └── config.json                  # Конфигурация (менеджеры, настройки)
│
├── modules/
│   ├── data_processor.py            # Обработка и валидация данных
│   ├── analytics.py                 # Анализ результатов из Битрикс24
│   ├── bitrix_mapper.py             # Маппинг данных для импорта в Битрикс24
│   ├── phone_validator.py           # Валидация и очистка номеров телефонов
│   ├── chart_generator.py           # Генерация графиков (matplotlib)
│   └── report_exporter.py           # Экспорт отчётов в Excel
│
├── gui/
│   ├── main_window.py               # Главное окно (CustomTkinter)
│   └── preview_table.py             # Компонент предпросмотра таблицы
│
├── utils/
│   ├── config_loader.py             # Работа с config.json
│   ├── url_cleaner.py               # Очистка URL от UTM-меток и других параметров отслеживания
│   └── logger.py                    # Логирование операций
│
├── database/
│   └── db_manager.py                # SQLite для истории обработок
│
├── data/
│   ├── processed/                   # Обработанные файлы
│   ├── reports/                     # Графики и отчёты
│   └── database.db                  # SQLite база данных
│
├── logs/
│   └── app_YYYY-MM-DD.log          # Логи по датам
│
└── requirements.txt                 # Зависимости Python
```

---

## 🔧 Технический стек

### Backend

- **Python 3.10+**
- **pandas** — обработка CSV файлов
- **phonenumbers** — валидация телефонных номеров
- **matplotlib** — генерация графиков
- **openpyxl** — работа с Excel файлами
- **sqlite3** (встроенная) — история обработок

### Frontend

- **customtkinter** — современный GUI интерфейс
- **tkinter** — базовые виджеты (Treeview для таблиц)

### Утилиты

- **logging** — логирование операций
- **json** — конфигурационные файлы
- **threading** — асинхронная обработка

---

## 📦 Установка и запуск

### 1. Клонирование проекта

```bash
git clone <repository-url>
cd lead-generation-system
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

**requirements.txt:**

```
pandas==2.1.4
phonenumbers==8.13.26
customtkinter==5.2.1
matplotlib==3.8.2
openpyxl==3.1.2
```

### 3. Запуск приложения

```bash
python main.py
```

---

## 🎯 Руководство пользователя

### Вкладка "📊 Обработка данных"

#### Шаг 1: Загрузка файлов

1. Нажмите **"Выбрать CSV файлы"**
2. Выберите один или несколько CSV файлов с контактами
3. Файлы отобразятся в списке (можно удалить ненужные)

#### Шаг 2: Настройка менеджеров

1. В правой панели введите список менеджеров (по одному на строку)
2. Нажмите **"💾 Сохранить список менеджеров"**
3. Список сохранится в config.json

#### Шаг 3: Обработка

1. Нажмите **"🔄 Очистить и объединить"**
2. Система выполнит:
   - Чтение всех CSV файлов
   - Валидацию телефонов (российские форматы)
   - Удаление дубликатов
   - Нормализацию данных
3. Результаты отобразятся в статистике

#### Шаг 4: Предпросмотр

- Нажмите **"👁️ Предпросмотр данных"** для просмотра обработанных записей

#### Шаг 5: Экспорт

1. Нажмите **"📥 Экспортировать для Битрикс24"**
2. Выберите место сохранения файла
3. Файл готов для импорта в Битрикс24

**Формат экспорта:**

- `Название` — имя компании/контакта
- `Телефон` — валидный номер телефона
- `Адрес` — адрес
- `Категория` — категория (Category_0)
- `Ответственный` — назначенный менеджер (случайный выбор)
- `Источник` — имя исходного CSV файла
- Дополнительные поля: `companyUrl`, `telegram`, `vkontakte`, `whatsapp`, `phone_2`

### Вкладка "📈 Аналитика"

#### Шаг 1: Загрузка экспортов из Битрикс24

1. Экспортируйте из Битрикс24 два файла:
   - **LEAD.csv** — экспорт сущности "Лиды"
   - **DEAL.csv** — экспорт сущности "Сделки"
2. Нажмите **"Выбрать файл"** для каждого типа

#### Шаг 2: Анализ

1. Нажмите **"🔍 Проанализировать данные"**
2. Система вычислит:
   - Общее количество загруженных лидов
   - Количество отказов + распределение причин
   - Количество сделок в работе
   - Успешные продажи
   - Конверсию (%)
   - Топ-менеджеров по сделкам

#### Шаг 3: Экспорт отчёта

1. Нажмите **"📊 Экспортировать отчёт (Excel)"**
2. Файл .xlsx будет содержать:
   - Общую статистику
   - Таблицы причин отказов
   - Таблицы стадий сделок
   - Топ-менеджеров
   - Графики (круговая диаграмма, столбчатая диаграмма)

### Дополнительные функции

#### 📜 История обработок

- Нажмите **"📜 История"** в верхней панели
- Просмотр последних 10 обработок с метриками

#### 🌙 Переключение темы

- Переключатель в правом верхнем углу
- **Тёмная тема** (по умолчанию)
- **Светлая тема**
- Выбор сохраняется в config.json

---

## 📊 Форматы данных

### Входные CSV файлы

**Требования:**

- Кодировка: UTF-8 или UTF-8-sig
- Разделители: `,` или `;` (автоопределение)
- Обязательные колонки:
  - `Название` — название компании/контакта
  - `phone_1` или `Телефон` — основной телефон
  - `Адрес` — адрес

**Опциональные колонки:**

- `phone_2` — дополнительный телефон
- `companyUrl` — сайт компании
- `telegram`, `vkontakte`, `whatsapp` — соцсети
- `Category_0` — категория

**Пример структуры:**

```csv
Название,Адрес,phone_1,phone_2,companyUrl,Category_0
ООО "Пример",Москва ул. Ленина 1,+79991234567,,https://example.com,Услуги
```

### Выходной CSV для Битрикс24

**Структура:**

```csv
Название,Телефон,Адрес,Категория,Ответственный,Источник,companyUrl,telegram,vkontakte,whatsapp,phone_2
```

**Особенности:**

- Все телефоны валидированы (российские форматы)
- Дубликаты по телефонам удалены
- Ответственный выбирается случайно из списка менеджеров
- Источник = имя исходного CSV файла

---

## 🔍 Валидация телефонов

### Поддерживаемые форматы

**Мобильные номера (Россия):**

- `+7 (999) 123-45-67`
- `8 999 123 45 67`
- `79991234567`
- `89991234567`

**Городские номера (Москва, Санкт-Петербург):**

- `+7 (495) 123-45-67` — Москва
- `+7 (812) 123-45-67` — Санкт-Петербург
- Также без форматирования: `74951234567`

**Правила валидации:**

1. Номер должен начинаться с `+7`, `7` или `8`
2. Мобильные: 11 цифр, код оператора 9XX
3. Городские: 11 цифр, код города 495/499/812
4. Некорректные символы игнорируются (скобки, тире, пробелы)

**Примеры невалидных номеров:**

- `123456` — слишком короткий
- `+1 (999) 123-45-67` — неверный код страны
- `+7 (700) 123-45-67` — неверный код оператора
- `nan`, `None`, пустая строка — отсутствие номера

---

## 📈 Метрики аналитики

### Общая статистика

- **Всего записей** — сумма лидов и сделок
- **В работе (DEAL)** — количество сделок
- **Отказы (LEAD)** — количество лидов с причиной отказа
- **Успешные продажи** — сделки в стадии "Успешно реализовано" / "Выиграно"
- **Конверсия (%)** — `(total_deals + successful_deals) / total_leads * 100`

### Причины отказа

- Топ-5 причин отказа с количеством и процентами
- Источник: колонка "Причина отказа" в LEAD.csv

### Стадии сделок

- Распределение сделок по стадиям воронки
- Источник: колонка "Стадия сделки" в DEAL.csv

### Топ-менеджеры

- Топ-3 менеджера по количеству сделок в работе
- Источник: колонка "Ответственный" в DEAL.csv

### Графики

1. **Круговая диаграмма** — распределение причин отказа
2. **Столбчатая диаграмма** — топ-менеджеры по сделкам

---

## 🗄️ База данных (SQLite)

**Файл:** `data/database.db`

### Таблица `processing_history`

```sql
CREATE TABLE processing_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    input_files TEXT,           -- JSON массив имён файлов
    output_file TEXT,            -- Имя выходного файла
    rows_processed INTEGER,      -- Всего строк обработано
    rows_output INTEGER,         -- Валидных строк
    duplicates_removed INTEGER,  -- Удалено дубликатов
    invalid_phones INTEGER,      -- Невалидных телефонов
    processing_time REAL,        -- Время обработки (сек)
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### Таблица `managers`

```sql
CREATE TABLE managers (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE,
    is_active INTEGER DEFAULT 1,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 📝 Логирование

**Файлы:** `logs/app_YYYY-MM-DD.log`

### Формат записи

```
2026-02-05 13:38:15 | INFO | Приложение запущено
2026-02-05 13:40:22 | INFO | Обработан файл: data.csv
2026-02-05 13:40:22 | INFO |   Всего строк: 1000
2026-02-05 13:40:22 | INFO |   Валидных: 850
2026-02-05 13:40:22 | INFO |   Дубликатов удалено: 100
2026-02-05 13:40:22 | INFO |   Невалидных телефонов: 50
2026-02-05 13:40:30 | INFO | Экспорт в Битрикс: bitrix_import.csv
2026-02-05 13:40:30 | INFO |   Строк экспортировано: 850
```

### Уровни логирования

- **INFO** — обычные операции
- **WARNING** — предупреждения (не критично)
- **ERROR** — ошибки (требуют внимания)

---

## ⚙️ Конфигурация (config.json)

**Файл:** `config/config.json`

```json
{
  "app_name": "Lead Generation System",
  "version": "3.0.0",
  "managers": [
    "Иван Иванов",
    "Петр Петров",
    "Анна Аннина",
    "Роман Катников",
    "Дмитрий Бурмистров"
  ],
  "paths": {
    "input": "data/input",
    "output": "data/output",
    "reports": "data/reports",
    "database": "data/database.db"
  },
  "settings": {
    "preview_rows": 10,
    "theme": "dark"
  }
}
```

### Параметры

- **managers** — список менеджеров для распределения лидов
- **theme** — тема интерфейса (`dark` или `light`)
- **export_format** — формат экспорта (сейчас только `bitrix24`)

---

## 🚀 Текущий статус разработки

### ✅ Этап 1: Ядро обработки данных (ЗАВЕРШЁН)

- ✅ Модуль `DataProcessor` — чтение, валидация, объединение CSV
- ✅ Валидация российских номеров (мобильные + городские)
- ✅ Дедупликация по телефонам
- ✅ Экспорт в формат Битрикс24

### ✅ Этап 2: GUI интерфейс (ЗАВЕРШЁН)

- ✅ Главное окно с вкладками (CustomTkinter)
- ✅ Вкладка обработки данных
- ✅ Выбор множественных файлов
- ✅ Редактор списка менеджеров
- ✅ Прогресс-бар и статистика
- ✅ Кнопки обработки и экспорта

### ✅ Этап 3: Аналитика и отчёты (ЗАВЕРШЁН)

- ✅ Модуль `Analytics` — загрузка LEAD.csv и DEAL.csv
- ✅ Подсчёт метрик (конверсия, отказы, сделки)
- ✅ Модуль `ChartGenerator` — генерация графиков (matplotlib)
- ✅ Модуль `ReportExporter` — экспорт в Excel с графиками
- ✅ Вкладка аналитики в GUI

### ✅ Вариант 3: Улучшения GUI (ЗАВЕРШЁН)

- ✅ Предпросмотр таблицы (Treeview с прокруткой)
- ✅ Логирование операций в файлы
- ✅ SQLite база данных для истории обработок
- ✅ Кнопка "История" с просмотром последних 10 операций
- ✅ Переключатель темы (светлая/тёмная)
- ✅ Сохранение настроек темы

### 🔄 Этап 4: Дополнительные функции (ПЛАНИРУЕТСЯ)

- ⏳ Генератор ссылок для Яндекс.Карт
- ⏳ Интеграция с Webbee AI API (автопарсинг)
- ⏳ Поиск ЛПР (Лиц, Принимающих Решения)
- ⏳ Интеграция с Битрикс24 API (прямая загрузка без CSV)

### 🎯 Следующие шаги

1. **Создание .exe файла** — упаковка приложения для распространения
2. **Тестирование на реальных данных** — проверка всех сценариев
3. **Реализация дополнительных функций** (по запросу)

---

## 🐛 Известные ограничения

1. **Валидация телефонов** — поддерживаются только российские номера
2. **CSV кодировка** — предпочтительна UTF-8 (UTF-8-sig также работает)
3. **Битрикс24 формат** — импорт только через CSV
4. **Графики** — автоматически сохраняются как PNG (встраиваются в Excel)
5. **История обработок** — хранится локально в SQLite (не синхронизируется)

---

## 📚 Примеры использования

### Пример 1: Обработка одного файла

```
1. Выбрать файл "contacts_2024.csv"
2. Ввести список менеджеров
3. Нажать "Очистить и объединить"
4. Результат: 1000 строк → 850 валидных → экспорт в bitrix_import.csv
```

### Пример 2: Объединение нескольких файлов

```
1. Выбрать файлы:
   - "moscow_leads.csv"
   - "spb_leads.csv"
   - "regions_leads.csv"
2. Система автоматически:
   - Объединит все файлы
   - Удалит дубликаты между файлами
   - Сохранит источник в колонке "Источник"
3. Экспорт: один файл с уникальными контактами
```

### Пример 3: Анализ результатов после загрузки в Битрикс24

```
1. Экспортировать из Битрикс24:
   - LEAD.csv (все лиды)
   - DEAL.csv (все сделки)
2. Загрузить файлы во вкладку "Аналитика"
3. Проанализировать:
   - Конверсия: 15%
   - Топ причина отказа: "Нет денег" (40%)
   - Лучший менеджер: "Иван Иванов" (50 сделок)
4. Экспортировать отчёт в Excel для презентации
```

---

## 🛠️ Устранение неполадок

### Проблема: "Файл не читается"

**Решение:**

- Проверьте кодировку файла (должна быть UTF-8)
- Откройте файл в Excel → "Сохранить как" → выберите "CSV UTF-8"

### Проблема: "Все телефоны невалидные"

**Решение:**

- Проверьте формат телефонов (должны начинаться с +7, 7 или 8)
- Убедитесь, что колонка называется `phone_1` или `Телефон`

### Проблема: "Не сохраняются менеджеры"

**Решение:**

- Проверьте права доступа к папке `config/`
- Убедитесь, что файл `config.json` не открыт в другой программе

### Проблема: "Не открывается история"

**Решение:**

- Проверьте наличие файла `data/database.db`
- Если файл повреждён — удалите его, система создаст новый

### Проблема: "Графики не отображаются в Excel"

**Решение:**

- Графики сохраняются как PNG в папке `data/reports/`
- Убедитесь, что matplotlib установлен: `pip install matplotlib`

---

## 📞 Контакты и поддержка

**Проект:** Lead Generation System v1.0  
**Создатель:** Иванн  
**Email:** <i@iac-iac.ru>  
**Дата создания:** 05.02.2026

---

## 📄 Лицензия

Проект создан для внутреннего использования компании. Все права защищены.
